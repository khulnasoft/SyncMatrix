apiVersion: apps/v1
kind: Deployment
metadata:
  name: syncmatrix-agent
  namespace: ${namespace}
  labels:
    app: syncmatrix-agent
spec:
  selector:
    matchLabels:
      app: syncmatrix-agent
  replicas: 1
  template:
    metadata:
      labels:
        app: syncmatrix-agent
    spec:
      containers:
        - name: agent
          image: ${image_name}
          command: ["syncmatrix", "agent", "start", "-q", "${work_queue}"]
          imagePullPolicy: "IfNotPresent"
          env:
            - name: SYNCMATRIX_API_URL
              value: ${api_url}
            - name: SYNCMATRIX_API_KEY
              value: ${api_key}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: syncmatrix-agent
  namespace: ${namespace}
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/log", "pods/status"]
    verbs: ["get", "watch", "list"]
  - apiGroups: ["batch"]
    resources: ["jobs"]
    verbs: [ "get", "list", "watch", "create", "update", "patch", "delete" ]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: syncmatrix-agent-role-binding
  namespace: ${namespace}
subjects:
  - kind: ServiceAccount
    name: default
    namespace: ${namespace}
roleRef:
  kind: Role
  name: syncmatrix-agent
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: syncmatrix-agent
rules:
  - apiGroups: [""]
    resources: ["namespaces"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: syncmatrix-agent-cluster-role-binding
subjects:
  - kind: ServiceAccount
    name: default
    namespace: ${namespace}
roleRef:
  kind: ClusterRole
  name: syncmatrix-agent
  apiGroup: rbac.authorization.k8s.io
